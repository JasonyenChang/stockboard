# FinMind API token (optional but strongly recommended).
# Without it the public endpoint is heavily rate-limited and may return HTTP 402.
# Register a free account at https://finmindtrade.com and copy your token here,
# then rename this file to .env.local
FINMIND_TOKEN=
